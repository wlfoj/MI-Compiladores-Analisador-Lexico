algoritmo principal(){
a
&&
variaveis{
	real f = 3.0;
	@
	texto msg = "teste"
}

tes#te
se ( f >= -10) && (f < ) {
	escreva("mensagem);
	escreva("menÇagem");
// exemplo de teste do lexico

3.3.4
/* exa869 mi processadores de
		linguagem de programacao
}